- name: Deploy App
  hosts: web
  become: yes

  tasks:

    - name: Stop and remove all containers using port 8080
      shell: |
        docker ps -q --filter "publish=8080" | xargs -r docker rm -f

    - name: Run new container
      shell: docker run -d -p 8080:8080 java-app